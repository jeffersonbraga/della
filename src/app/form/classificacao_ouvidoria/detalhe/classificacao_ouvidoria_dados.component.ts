import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'classificacao_ouvidoria-dados',   templateUrl: 'classificacao_ouvidoria_dados.component.html' }) export class Classificacao_ouvidoriaDadosComponent implements OnInit {       displayMode: Classificacao_ouvidoriaDadosDisplayModeEnum;     displayModeEnum = Classificacao_ouvidoriaDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Classificacao_ouvidoriaDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Classificacao_ouvidoriaDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Classificacao_ouvidoriaDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Classificacao_ouvidoriaDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }