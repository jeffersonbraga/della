import { Routes, RouterModule } from '@angular/router';  import { Checagem_documentoComponent } from './checagem_documento.component';  const checagem_documento_routes: Routes = [   { path: 'checagem_documento*', pathMatch:'full', redirectTo: '/checagem_documento' },   { path: 'checagem_documento', component: Checagem_documentoComponent} ];  export const checagem_documento_routing = RouterModule.forChild(checagem_documento_routes);