import { Component, Input, OnInit } from '@angular/core'; import { Router } from '@angular/router'; import { Config_indicadores } from '../../../modelo/Config_indicadores'; import { ObjetoVirtual } from '../../../modelo/ObjetoVirtual';  import { PesquisaService } from '../../../core/services/pesquisa/pesquisa.service';  @Component({   moduleId: module.id,   selector: 'config_indicadores-selector',   templateUrl: 'config_indicadores.component.html' })  export class Config_indicadoresComponent implements OnInit { 	 	errorMessage : string = '';      @Input() listaDados : Config_indicadores[] = [];     config_indicadoresAux : Config_indicadores;     constructor(        private dataService: PesquisaService,        private router: Router) { }      ngOnInit() {         this.config_indicadoresAux = new Config_indicadores(); let dataAux : ObjetoVirtual = new ObjetoVirtual();         dataAux.classeJava  = 'br.com.primum.modelo.config_indicadores.Config_indicadores';         dataAux.objetoJson = this.config_indicadoresAux;          this.dataService.efetuarPesquisa(dataAux)             .subscribe(             result => this.onEfetuarPesquisa(result),             error =>  this.errorMessage = <any>error);     }  	onEfetuarPesquisa(result : any) { 		if (!result) { 			this.errorMessage = 'Houve um erro a pesquisa de Config_indicadores. ';             this.router.navigate(['/home']); 		}          this.listaDados = result; 	} }