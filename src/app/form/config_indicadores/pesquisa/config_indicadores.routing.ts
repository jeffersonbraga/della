import { Routes, RouterModule } from '@angular/router';  import { Config_indicadoresComponent } from './config_indicadores.component';  const config_indicadores_routes: Routes = [   { path: 'config_indicadores*', pathMatch:'full', redirectTo: '/config_indicadores' },   { path: 'config_indicadores', component: Config_indicadoresComponent} ];  export const config_indicadores_routing = RouterModule.forChild(config_indicadores_routes);