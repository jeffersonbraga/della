import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'operacao_notificacao-dados',   templateUrl: 'operacao_notificacao_dados.component.html' }) export class Operacao_notificacaoDadosComponent implements OnInit {       displayMode: Operacao_notificacaoDadosDisplayModeEnum;     displayModeEnum = Operacao_notificacaoDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Operacao_notificacaoDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Operacao_notificacaoDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Operacao_notificacaoDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Operacao_notificacaoDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }