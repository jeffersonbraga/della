import { Routes, RouterModule } from '@angular/router';  import { Operacao_notificacaoComponent } from './operacao_notificacao.component';  const operacao_notificacao_routes: Routes = [   { path: 'operacao_notificacao*', pathMatch:'full', redirectTo: '/operacao_notificacao' },   { path: 'operacao_notificacao', component: Operacao_notificacaoComponent} ];  export const operacao_notificacao_routing = RouterModule.forChild(operacao_notificacao_routes);