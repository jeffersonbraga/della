import { Routes, RouterModule } from '@angular/router';  import { Indicador_customizadoComponent } from './indicador_customizado.component';  const indicador_customizado_routes: Routes = [   { path: 'indicador_customizado*', pathMatch:'full', redirectTo: '/indicador_customizado' },   { path: 'indicador_customizado', component: Indicador_customizadoComponent} ];  export const indicador_customizado_routing = RouterModule.forChild(indicador_customizado_routes);