import { Routes, RouterModule } from '@angular/router';  import { Sub_formularioComponent } from './sub_formulario.component';  const sub_formulario_routes: Routes = [   { path: 'sub_formulario*', pathMatch:'full', redirectTo: '/sub_formulario' },   { path: 'sub_formulario', component: Sub_formularioComponent} ];  export const sub_formulario_routing = RouterModule.forChild(sub_formulario_routes);