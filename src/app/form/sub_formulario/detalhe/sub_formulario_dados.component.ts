import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'sub_formulario-dados',   templateUrl: 'sub_formulario_dados.component.html' }) export class Sub_formularioDadosComponent implements OnInit {       displayMode: Sub_formularioDadosDisplayModeEnum;     displayModeEnum = Sub_formularioDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Sub_formularioDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Sub_formularioDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Sub_formularioDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Sub_formularioDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }