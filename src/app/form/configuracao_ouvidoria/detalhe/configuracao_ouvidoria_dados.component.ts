import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'configuracao_ouvidoria-dados',   templateUrl: 'configuracao_ouvidoria_dados.component.html' }) export class Configuracao_ouvidoriaDadosComponent implements OnInit {       displayMode: Configuracao_ouvidoriaDadosDisplayModeEnum;     displayModeEnum = Configuracao_ouvidoriaDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Configuracao_ouvidoriaDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Configuracao_ouvidoriaDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Configuracao_ouvidoriaDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Configuracao_ouvidoriaDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }