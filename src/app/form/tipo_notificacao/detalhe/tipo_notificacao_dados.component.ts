import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'tipo_notificacao-dados',   templateUrl: 'tipo_notificacao_dados.component.html' }) export class Tipo_notificacaoDadosComponent implements OnInit {       displayMode: Tipo_notificacaoDadosDisplayModeEnum;     displayModeEnum = Tipo_notificacaoDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Tipo_notificacaoDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Tipo_notificacaoDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Tipo_notificacaoDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Tipo_notificacaoDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }