import { Routes, RouterModule } from '@angular/router';  import { PeriodicidadeComponent } from './periodicidade.component';  const periodicidade_routes: Routes = [   { path: 'periodicidade*', pathMatch:'full', redirectTo: '/periodicidade' },   { path: 'periodicidade', component: PeriodicidadeComponent} ];  export const periodicidade_routing = RouterModule.forChild(periodicidade_routes);