import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'mapa_formulario-dados',   templateUrl: 'mapa_formulario_dados.component.html' }) export class Mapa_formularioDadosComponent implements OnInit {       displayMode: Mapa_formularioDadosDisplayModeEnum;     displayModeEnum = Mapa_formularioDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Mapa_formularioDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Mapa_formularioDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Mapa_formularioDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Mapa_formularioDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }