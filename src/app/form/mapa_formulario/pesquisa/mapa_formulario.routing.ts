import { Routes, RouterModule } from '@angular/router';  import { Mapa_formularioComponent } from './mapa_formulario.component';  const mapa_formulario_routes: Routes = [   { path: 'mapa_formulario*', pathMatch:'full', redirectTo: '/mapa_formulario' },   { path: 'mapa_formulario', component: Mapa_formularioComponent} ];  export const mapa_formulario_routing = RouterModule.forChild(mapa_formulario_routes);