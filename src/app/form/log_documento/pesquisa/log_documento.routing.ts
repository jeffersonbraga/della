import { Routes, RouterModule } from '@angular/router';  import { Log_documentoComponent } from './log_documento.component';  const log_documento_routes: Routes = [   { path: 'log_documento*', pathMatch:'full', redirectTo: '/log_documento' },   { path: 'log_documento', component: Log_documentoComponent} ];  export const log_documento_routing = RouterModule.forChild(log_documento_routes);