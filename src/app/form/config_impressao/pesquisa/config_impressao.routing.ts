import { Routes, RouterModule } from '@angular/router';  import { Config_impressaoComponent } from './config_impressao.component';  const config_impressao_routes: Routes = [   { path: 'config_impressao*', pathMatch:'full', redirectTo: '/config_impressao' },   { path: 'config_impressao', component: Config_impressaoComponent} ];  export const config_impressao_routing = RouterModule.forChild(config_impressao_routes);