import { Routes, RouterModule } from '@angular/router';  import { FluxogramaDadosComponent }   from './fluxograma_dados.component'; import { FluxogramaDadosDetailsComponent } from './fluxograma_dadosDetails.component'; import { FluxogramaDadosEditComponent } from './fluxograma_dadosEdit.component';  const fluxograma_dados_routes: Routes = [   {     path: '',     component: FluxogramaDadosComponent,     children: [       { path:'details', component: FluxogramaDadosDetailsComponent },       { path:'edit', component: FluxogramaDadosEditComponent }     ]   } ];  export const fluxograma_dados_routing = RouterModule.forChild(fluxograma_dados_routes); 