import { Routes, RouterModule } from '@angular/router';  import { Formulario_indicadorComponent } from './formulario_indicador.component';  const formulario_indicador_routes: Routes = [   { path: 'formulario_indicador*', pathMatch:'full', redirectTo: '/formulario_indicador' },   { path: 'formulario_indicador', component: Formulario_indicadorComponent} ];  export const formulario_indicador_routing = RouterModule.forChild(formulario_indicador_routes);