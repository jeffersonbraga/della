import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'unidade-dados',   templateUrl: 'unidade_dados.component.html' }) export class UnidadeDadosComponent implements OnInit {       displayMode: UnidadeDadosDisplayModeEnum;     displayModeEnum = UnidadeDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = UnidadeDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = UnidadeDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = UnidadeDadosDisplayModeEnum.Edit;           break;       }     }  }  enum UnidadeDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }