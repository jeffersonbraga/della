import { Component, OnInit } from '@angular/core'; import { Router, ActivatedRoute } from '@angular/router'; import { Subscription } from 'rxjs/Subscription';  import { Acao_corretiva_helpdesk } from '../../../modelo/Acao_corretiva_helpdesk'; import { ObjetoVirtual } from '../../../modelo/ObjetoVirtual'; import { PesquisaService } from '../../../core/services/pesquisa/pesquisa.service';  @Component({   moduleId: module.id,   selector: 'acao_corretiva_helpdesk-dados-edit',   templateUrl: 'acao_corretiva_helpdesk_dadosEdit.component.html' }) export class Acao_corretiva_helpdeskDadosEditComponent implements OnInit {    acao_corretiva_helpdesk: Acao_corretiva_helpdesk;    private sub: Subscription;    private errorMessage: string;     constructor(private router: Router,               private route: ActivatedRoute,               private pesquisaService: PesquisaService) { }    ngOnInit() {       this.sub = this.route.parent.params.subscribe(params => {         let o : ObjetoVirtual = new ObjetoVirtual();         o.classeJava = "br.com.primum.modelo.acao_corretiva_helpdesk.Acao_corretiva_helpdesk";                 let id = +params['id'];         this.acao_corretiva_helpdesk = new Acao_corretiva_helpdesk();         this.acao_corretiva_helpdesk.cd_acao_corretiva_helpdesk = id;         o.objetoJson = this.acao_corretiva_helpdesk;          this.pesquisaService.efetuarPesquisa(o)             .subscribe(             result => this.onEfetuarPesquisa(result),             error =>  this.errorMessage = <any>error);       });   }    onEfetuarPesquisa(result : any) {     if (!result) {       this.errorMessage = 'Houve um erro ao pesquisa a acao_corretiva_helpdesk. ';       }      this.acao_corretiva_helpdesk = result;   }    ngOnDestroy() {     this.sub.unsubscribe();   }     onSubmit() {         }     onCancel(event: Event) {     event.preventDefault();     this.router.navigate(['/']);   }  }