import { Routes, RouterModule } from '@angular/router';  import { Acao_corretiva_formularioComponent } from './acao_corretiva_formulario.component';  const acao_corretiva_formulario_routes: Routes = [   { path: 'acao_corretiva_formulario*', pathMatch:'full', redirectTo: '/acao_corretiva_formulario' },   { path: 'acao_corretiva_formulario', component: Acao_corretiva_formularioComponent} ];  export const acao_corretiva_formulario_routing = RouterModule.forChild(acao_corretiva_formulario_routes);