import { Routes, RouterModule } from '@angular/router';  import { Programa_qualidadeComponent } from './programa_qualidade.component';  const programa_qualidade_routes: Routes = [   { path: 'programa_qualidade*', pathMatch:'full', redirectTo: '/programa_qualidade' },   { path: 'programa_qualidade', component: Programa_qualidadeComponent} ];  export const programa_qualidade_routing = RouterModule.forChild(programa_qualidade_routes);