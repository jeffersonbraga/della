import { Routes, RouterModule } from '@angular/router';  import { Container_principalComponent } from './container_principal.component';  const container_principal_routes: Routes = [   { path: 'container_principal*', pathMatch:'full', redirectTo: '/container_principal' },   { path: 'container_principal', component: Container_principalComponent} ];  export const container_principal_routing = RouterModule.forChild(container_principal_routes);