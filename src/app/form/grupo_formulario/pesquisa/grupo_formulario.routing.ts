import { Routes, RouterModule } from '@angular/router';  import { Grupo_formularioComponent } from './grupo_formulario.component';  const grupo_formulario_routes: Routes = [   { path: 'grupo_formulario*', pathMatch:'full', redirectTo: '/grupo_formulario' },   { path: 'grupo_formulario', component: Grupo_formularioComponent} ];  export const grupo_formulario_routing = RouterModule.forChild(grupo_formulario_routes);