import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'grupo_formulario-dados',   templateUrl: 'grupo_formulario_dados.component.html' }) export class Grupo_formularioDadosComponent implements OnInit {       displayMode: Grupo_formularioDadosDisplayModeEnum;     displayModeEnum = Grupo_formularioDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Grupo_formularioDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Grupo_formularioDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Grupo_formularioDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Grupo_formularioDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }