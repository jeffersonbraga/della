import { Routes, RouterModule } from '@angular/router';  import { SistemaDadosComponent }   from './sistema_dados.component'; import { SistemaDadosDetailsComponent } from './sistema_dadosDetails.component'; import { SistemaDadosEditComponent } from './sistema_dadosEdit.component';  const sistema_dados_routes: Routes = [   {     path: '',     component: SistemaDadosComponent,     children: [       { path:'details', component: SistemaDadosDetailsComponent },       { path:'edit', component: SistemaDadosEditComponent }     ]   } ];  export const sistema_dados_routing = RouterModule.forChild(sistema_dados_routes); 