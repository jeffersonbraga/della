import { Component, OnInit } from '@angular/core'; import { Router } from '@angular/router';  @Component({   moduleId: module.id,   selector: 'escala_tempo-dados',   templateUrl: 'escala_tempo_dados.component.html' }) export class Escala_tempoDadosComponent implements OnInit {       displayMode: Escala_tempoDadosDisplayModeEnum;     displayModeEnum = Escala_tempoDadosDisplayModeEnum;       constructor(private router: Router) { }      ngOnInit() {       const path = this.router.url.split('/')[3];       switch (path) {         case 'details':           this.displayMode = Escala_tempoDadosDisplayModeEnum.Details;           break;         case 'info':           this.displayMode = Escala_tempoDadosDisplayModeEnum.Orders;           break;         case 'edit':           this.displayMode = Escala_tempoDadosDisplayModeEnum.Edit;           break;       }     }  }  enum Escala_tempoDadosDisplayModeEnum {   Details=0,   Orders=1,   Edit=2 }